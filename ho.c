#inclued <stdio.h>

int main(){
	char clock[10];
	int price;

	printf("Enter a clock : ");
	scanf("%s", &clock);

	printf("enter the clock price: ");
	scanf("%d", &price); 

	printf("the name entered is: %s\n", name);

	if price > 

return 0;
}